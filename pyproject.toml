[project]
name = "databow"
version = "0.1.0-alpha"
requires-python = ">=3.10"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
